print('Montu Miar Ovijan', 'Montu Mia Nayok', sep=' **** ')
# Variable
full_name = 'Solemon khan'
age = 18
print(full_name, 'is', age, 'years old')

# Variable Naming Convention
# 1. Variable name must be start with letter or underscore
# 2. can't start with number
# 3. have only alphabet or numeric or underscore
#  in python we use snake case : full_name

# Number (integer- 1,-5,6, float-2.300, complex - 2j)
# string = '' / ""
# boolean = True, False
# +,-,*,/ -- // == Floor Division(11 // 3 = 3),
# ** = exponent (2**3 = 2*2*2),
# % - modulus Operator - 11 % 3 == 2
# data casting - str(number), int(str/float), float(str,int)
